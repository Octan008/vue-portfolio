module.exports = {
  "transpileDependencies": [
    "vuetify"
  ],
  publicPath: process.env.NODE_ENV === 'production'
      ? '/vue-portfolio/'
      : '/',
  assetsDir: '',
  outputDir:'docs',

  
  
}